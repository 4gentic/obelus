\documentclass{article}
\begin{document}

\section{Introduction}

We claim the covariance matrix is positive semi-definite under the standard
regularity conditions.

\section{Method}

The estimator is unbiased. The proof follows from the construction in
Section~\ref{sec:proof}.

\section{Proof}\label{sec:proof}

The matrix decomposes as $Z = A^T A$, which is PSD by inspection.

\end{document}
