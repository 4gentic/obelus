# A short paper

We claim the covariance matrix Z is positive semi-definite.

## Method

The estimator is unbiased under the standard regularity conditions.

> A note in passing: this matters because downstream inference relies on it.

- first observation
- second observation
- third observation

```python
def estimate(samples):
    return samples.mean(axis=0)
```

| metric | value |
| ------ | ----- |
| MSE    | 0.04  |
| RMSE   | 0.20  |
