name: ci
on:
  pull_request:
    branches:
      - 'release-please--**'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release Please PR — verify skipped"
