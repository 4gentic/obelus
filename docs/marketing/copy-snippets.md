# Reusable copy

Short, composable strings. Keep them dry; reach for them whenever launch-day copy needs to be consistent.

## GitHub description

An offline review surface for AI-assisted papers. Mark passages in PDF, Markdown (Beta), or HTML (Beta); export a bundle a Claude Code plugin applies to your `.tex` / `.md` / `.typ` / `.html` source.

## Landing hero

Headline (two lines):

> Writing a paper with AI is cheap.
> Reviewing it is the work.

Subhead (one line):

> Mark the passages you doubt; Obelus hands the marks off to Claude Code, which edits the source.

## Install CTA

Open obelus.4gentic.ai in a browser, or install the plugin in your paper's repository with `/plugin marketplace add 4gentic/obelus` followed by `/plugin install obelus@4gentic`.

## Privacy one-liner

Papers stay on the device — PDFs in OPFS, annotations in IndexedDB, external assets stripped before they reach the DOM. Zero runtime network.

## README opening

Obelus is an offline review surface for AI-assisted papers. You highlight passages in any PDF, Markdown (Beta), or HTML (Beta) paper; it exports a bundle that a Claude Code plugin applies to your LaTeX, Markdown, Typst, or HTML source.

## 4gentic imprint

A project by [4gentic](https://4gentic.ai).
